# 無人島脱出（アイドルアーケード試作）

Whiteout Survival の広告に出てくるような「狩る → 運ぶ → 施設に投資する」ループを
そのまま遊べる形にした試作。現状は `index.html` 単体で完結している。
ブラウザで直接開けば動く。ビルド不要、外部ライブラリなし。

## ゲームループ

1. 柵の外（雪原）でクマに接触すると自動で戦闘
2. 倒すと肉が落ちる。歩いて拾うと頭上に積み上がる（運搬上限あり）
3. 拠点の厨房に肉を降ろすと住民が食べてコインが飛び出す
4. コインを拾い、施設の丸（投資パッド）の上に留まると投資が進む
   （必要額が大きい施設ほど1回の投入枚数が増えるので、一周にかかる時間は頭打ちになる）
5. リングが一周するとレベルアップ。効果が即時反映される
6. 全11施設が Lv3 以上になると船着き場が解禁され、投資しきると脱出＝クリア

## 雪原の3層

| 層 | y座標 | 敵 | HP | 攻撃 | 肉 |
|---|---|---|---|---|---|
| 浅場 | 196–274 | シロクマ | 40 | 2 | 2 |
| 中層 | 100–186 | 大グマ | 95 | 5 | 3 |
| 深部 | 22–90 | 氷牙グマ | 190 | 10 | 5 |

クマは層の外に出ない（`b.y` を層ごとにクランプ）。
生きている個体だけを `bears` 配列に置き、倒れた瞬間に `splice` する。

湧きは層ごとの補充クールダウン `scd[t]` で管理する。毎フレーム不足数を数え、
`scd[t]` が 0 になっていれば1頭足す。層が全滅していれば15フレーム間隔、
1〜2頭欠けているだけなら45フレーム間隔。特定の tick でしか判定しない作りだと、
兵士が湧いた端から狩る状況で層が空のまま止まって見えるため、この形にしている。

湧く位置は層内の候補を6点引いて、自分と兵士から最も遠い点を選ぶ。
兵士の足元に湧いて即狩られる（＝湧いていないように見える）のを防ぐため。

クマは自分が届かない相手を追わない（`hitTargets` の `reach`）。
これがないと拠点内の兵士を追って柵際に張り付き、生きたまま枠を占有して
補充が永久に止まる。襲撃個体は柵を越えるので `reach` の対象外。

### 座標を NaN にしない（重要）

**距離で割る前に必ず距離が 0 でないことを確かめる。** ここを踏むと
「クマが湧かなくなる」という形でバグが表に出る。経緯：

`hitTargets` の追跡ベクトルは `(tgt.x - e.x) / td` で、`td` は相手との距離。
`seek()` は残り距離が速度以下のとき相手の座標にピタリと重なるので、
兵士がクマに乗った次のフレームに `td === 0` になり `0/0 = NaN` が入る。
`Math.max`/`Math.min` は NaN をそのまま返すので、クランプを通しても直らない。

NaN 座標のクマは、描画されず・`d2()` が全部 false になるので誰にも倒せず・
`hp > 0` のまま `bears` に residue として残り続ける。`count(t)` はそれを数えるため、
**その層の補充が永久に止まる**。層ごとに順に死んでいく。

現在の防御は3段：

- `seek(u,tx,ty,sp,keep)` の `keep` で相手の手前に止まる（重なりを作らない）
- `hitTargets` は `td > 0.5` のときだけ割る
- クマ／襲撃個体のループで毎フレーム `isFinite` を確認し、崩れていたら層内に置き直す。
  復旧回数はデバッグ表示の「座標復旧」に出る。ここが増えるなら新しい NaN 源がある

## 兵士の行動

毎フレーム上から順に判定し、当てはまった時点で `continue` する優先順位制。

1. 体力が35%未満なら退却（`rest`）。85%まで回復したら復帰
2. 荷降ろし中（`dump`）なら厨房へ。**空になるまで降ろし切る**
3. 届く範囲に肉があれば拾いに行く
4. 到達層のクマがいれば攻撃
5. 何もなければ柵の内側の待機点へ

`dump` は `carry >= scap` で立ち、`carry <= 0` で降りる。
ここを「満載かどうか」だけで判定すると、1個降ろした瞬間に条件から外れて
**`scap - 1` 個を頭に載せたまま狩りに戻る**。倉庫を上げるほど残る数が増え、
往復1回につき1個しか納品しないので収入も大きく目減りする。
状態として持たせているのはこのため。自分（プレイヤー）の荷降ろしも同じ考え方。

兵士はクマの手前 `半径 + 10` で止まる（`seek` の `keep`）。
攻撃判定は `半径 + 17` なので攻撃は届く。重なりを作らないためのもの。
理由は「座標を NaN にしない」を参照。

### 兵士のランク

兵士は個体ごとに討伐数 `kills` を持ち、そこからランクが決まる。

| ランク | 討伐数 | 到達層 | 体力の加算 | 見た目 |
|---|---|---|---|---|
| 新兵 | 0〜7   | 浅場 | +0  | 緑・●    |
| 古参 | 8〜23  | 中層 | +20 | 青・●● ＋外周リング |
| 精鋭 | 24〜   | 深部 | +40 | 紫・●●● ＋外周リング |

`lv` は `kills` から `rankOf()` で導く従属値で、独立した状態ではない。
昇格の検出（ポップアップと全快）のために `u.lv` に持たせているだけなので、
`kills` を書き換えたら `lv` も更新すること。生成は必ず `mkSold(kills)` を通す。

`depth(u)` が到達層の下限y、`frontier()` が全兵士のうち最も深い到達線
（画面の破線）。**wep は攻撃力専任**で到達層には関与しない。

討伐の加算は兵士がとどめを刺したときだけ（`gain(u)`）。同じフレームに
別の兵士が撃ち込んで二重に数えないよう、倒れたクマに `done` を立てる。
クマの除去は従来どおり次フレームの `bears` ループなので、`done` は
その1フレームぶんの重複防止用。

倒れた兵士は `grave` に `kills` の半分を預け、医療での復活時に
`mkSold(k)` で戻る。精鋭が倒れると古参として復帰する。

## 施設（11種）

| ID | 名前 | 効果 |
|---|---|---|
| val | 厨房 | 肉の単価 +2、住民が1人増える |
| prc | 加工場 | 干し肉化で単価 +3 |
| tra | 訓練所 | 兵士が1人増える |
| wep | 兵器開発 | 自分と兵士の攻撃力上昇（到達層には関与しない） |
| cap | 倉庫 | 自分と兵士の運搬数 +1 |
| med | 医療 | 兵士の体力上昇、倒れた兵士が復活する |
| trd | 交易所 | 5秒ごとに自動収入 |
| wat | 見張り台 | 深部と襲撃個体に目印が付く |
| trp | 罠場 | 浅場に定期的に肉が湧く |
| wal | 防壁 | 襲撃クマの柵越えを阻止 |
| dck | 船着き場 | 他10施設がLv3以上で解禁。クリア条件 |

## 主要な数式

```
自分の攻撃力   = 8 + 兵器Lv * 6      （16フレームごと）
兵士の攻撃力   = 8 + 兵器Lv * 5      （12フレームごと）
兵士の体力     = 60 + 医療Lv * 30 + (ランク - 1) * 20
肉の単価       = (5 + 厨房Lv*2) + 加工場Lv*3
兵士のランク   = 討伐8で古参、討伐24で精鋭   （しきい値は RANKX）
兵士の到達層   = 新兵→浅場 / 古参→中層 / 精鋭→深部
投資コスト     = 施設のbase * (現在Lv + 1)
投資の投入枚数 = max(1, ceil(必要額 / 40))   （4フレームごと＝毎秒15回）
```

投入枚数を必要額に比例させているので、リング一周は必要額が大きくても約2.7秒で頭打ち。
安い施設（必要額40以下）は1枚ずつで、以前と同じ速さ。
所持金が足りなければある分だけ入り、端数はちょうど必要額で止まる。

## 襲撃

- 施設の合計Lvが10未満のあいだは発生しない
- 3秒の予告のあと出現。以降は約60秒間隔
- 合計Lv22未満はシロクマ、38未満は大グマ、それ以上は氷牙グマ
- 防壁Lv1以上なら柵で止まる。Lv0だと侵入して厨房から所持金を奪う

## 実装メモ

- 全部 `index.html` の1ファイル。`step()` が状態更新、`draw()` が描画、`loop()` が両方を回す
- 座標系は canvas 内部解像度 360x576 固定。表示は CSS で伸縮し、`pos()` が入力座標を換算する
- 絵はすべて Canvas の図形描画。画像素材は未使用
- デバッグ表示のチェックボックスで、クマの内訳・肉の数・襲撃までの秒数などが見える
- デバッグの「湧き待ち a/b/c」は層ごとの補充カウントダウン。
  「座標復旧 N回」は NaN 座標を直した回数で、**0のままなのが正常**

## 次にやること：ランクのしきい値を実測で詰める

実装は済んだので、残りは数値。ヘッドレスで回した結果は「変更履歴 2026-09-08」参照。
現在の 8 / 24 は昇格が速く、約2分で全員が精鋭になって個体差が消える。
「放置で古株が育つ」を効かせたいなら 12 / 40 あたりまで上げる余地がある。
触るのは `RANKX` の1行だけ。

## 既知の課題

- **バランス未調整**。特に交易所と罠場が強すぎる可能性が高い。実測して数値を詰める
- **兵士の昇格が速い**。上の「次にやること」を参照
- **精鋭が深部で足踏みする**。wep0 の兵士の攻撃力は8、氷牙グマはHP190なので
  倒し切る前に体力35%を割って退却する。ランクで到達層が開くぶん、
  wep への投資が実質的な前提になっている。これを狙いどおりとするか要判断
- **絵が素朴すぎる**。広告ゲームの手触りを出すには画像素材への差し替えが必要
- セーブ機能がない。閉じると最初から
- 放置報酬（閉じている間の収入）が未実装
- 住民に役割の区別がない。料理人・木こりなどに分けると施設を建てる理由が増える
- `step()` が長い。ファイル分割（entities / systems / render）を検討
- 音がない

## 変更履歴

### 2026-09-08

**兵士のランクを実装（設計2点を決定して着手）**

- ① 昇格方式は「戦闘経験で個別に昇格」を採用。討伐数 `kills` からランクを導く
- ② wep は攻撃力専任にし、到達層はランクへ移した

`depth()` は兵士1体を受け取る形に変更。`shp()` も同様（ランクで体力が伸びるため）。
生成は `mkSold(kills)` に集約し、訓練所・医療復活の2箇所とも通している。
詳細は「兵士のランク」。

**ヘッドレスでの実測（3人・wep0・med0）**

DOM と canvas をスタブして `step()` を回し、3600／10800フレームぶんを確認した。

- 座標復旧 0回、NaN 座標 0体。ランク導入で NaN 源は増えていない
- クマの補充は 3/3/2 で頭打ちのまま。兵士が深部へ行っても層が空にならない
- 昇格の速さは 新兵→古参 が約50秒、古参→精鋭 が約2分。3分で3人とも精鋭
- 3分放置で戦死ゼロ。退却の閾値（35%）が効いていて、深部でも死なずに往復する

### 2026-09-07

**クマがリスポーンしなくなる問題（解決・動作確認済み）**

真因は湧き処理ではなく、NaN 座標になったクマが層の枠を占有し続けていたこと。
詳細は「座標を NaN にしない」。`seek` の `keep`、`td > 0.5` の除算ガード、
毎フレームの `isFinite` 復旧の3段で塞いだ。

同時に湧き処理も作り直した（`tick` の位相判定 → 層ごとのクールダウン `scd`、
湧き位置を自分と兵士から離す）。こちらは真因ではないが、
兵士が湧いた端から狩ると層が空に見える問題は解消している。

**兵士が肉を全部降ろさない（解決）**

厨房へ運ぶ分岐が「満載か」だけを見ていたため、1個降ろすと条件から外れていた。
`dump` 状態を追加。詳細は「兵士の行動」。

**投資速度が必要額に追いつかない（解決）**

1回1枚固定だったのを `ceil(必要額 / 40)` 枚に。詳細は「主要な数式」。

## 作業を頼むときのコツ

修正を依頼するときは「何をしたら」「何が起きたか」をセットで伝えると原因を絞りやすい。
例：「兵士を3人にして5分放置したら、浅場のクマが0のまま増えなくなった」
